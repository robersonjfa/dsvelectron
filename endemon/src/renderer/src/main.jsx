import React from 'react'
import ReactDOM from 'react-dom/client'
import CasoPage from './pages/CasoPage.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CasoPage />
  </React.StrictMode>
)
